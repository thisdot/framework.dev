import classNames from "classnames"
import React from "react"
import { {{camelCase name}}Style } from "./{{kebabCase name}}.css"

export interface {{pascalCase name}}Props
	extends React.ComponentPropsWithoutRef<"{{el}}"> {}

export function {{pascalCase name}}({ children, className, ...props }: {{pascalCase name}}Props) {
	return (
		<{{el}} className={classNames(className, {{camelCase name}}Style)} {...props}>
			{children}
		</{{el}}>
	)
}