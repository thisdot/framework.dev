import React from "react"
import { IconProps } from "./shared-icon-types"

export const {{pascalCase name}}Icon = React.forwardRef<SVGSVGElement, IconProps>(
	function {{pascalCase name}}Icon(props, ref) {
		return (
			<svg viewBox={/* insert viewbox here */} fill="currentColor" ref={ref} {...props}>
				{/* insert svg contents here, stripped of any style properties */}
			</svg>
		)
	}
)