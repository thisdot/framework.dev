import classNames from "classnames"
import React from "react"
import { iconStyle } from "./icon.css"
import { type IconProps } from "./shared-icon-types"

export const {{pascalCase name}}Icon = React.forwardRef<SVGSVGElement, IconProps>(
	function {{pascalCase name}}Icon({ className, size, ...props }, ref) {
		return (
			<svg
				className={classNames(className, iconStyle({ size }))}
				viewBox={/* insert viewbox here */}
				fill="currentColor"
				ref={ref}
				{...props}
			>
				{/* insert svg contents here, stripped of any style properties */}
			</svg>
		)
	}
)
