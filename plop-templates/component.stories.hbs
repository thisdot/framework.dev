import { type Story, type Meta } from "@storybook/react"
import { {{pascalCase name}} as {{pascalCase name}}Component, {{pascalCase name}}Props } from "./{{kebabCase name}}"

export default {
	title: "{{startCase name}}",
	component: {{pascalCase name}}Component,
	args: {
		children: "Hello world",
	}
} as Meta

const Template: Story<{{pascalCase name}}Props> = (args) => <{{pascalCase name}}Component {...args} />

export const {{pascalCase name}} = Template.bind({})
